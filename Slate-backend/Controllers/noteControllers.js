const pool = require(".././db");

// POST - Create note
const createNote = () => {};

// GET - Get all note
const getAllNote = () => {};

// GET - Get note from id
const getNote = () => {};

// PUT - Update note
const updateNote = () => {};

// DELETE - Delete note
const deleteNote = () => {};

module.exports = {
  createNote,
  getAllNote,
  getNote,
  updateNote,
  deleteNote,
};
